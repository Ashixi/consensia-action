name: 'Consensia AI Reviewer'
description: 'AI Code Reviewer using multi-agent consensus to provide high-quality inline feedback.'
branding:
  icon: 'check-circle'
  color: 'blue'
inputs:
  api-key:
    description: 'Consensia CLI API Key'
    required: true
  github-token:
    description: 'GitHub token for posting comments and committing fixes'
    required: true
  target:
    description: 'What to review: "pr" (Pull Request) or "commit" (Direct Push)'
    required: true
  mode:
    description: 'Review mode (ECONOMY, BALANCED, MAX_POWER)'
    required: false
    default: 'BALANCED'
  rounds:
    description: 'Number of consensus rounds'
    required: false
    default: '2'
  api-url:
    description: 'Custom API URL (for self-hosting or testing)'
    required: false
    default: 'https://api.consensia.world/cli/analyze-diff' 
  fail-on-critical:
    description: 'Should the build be terminated if CRITICAL errors are found?'
    required: false
    default: 'false'
runs:
  using: "composite"
  steps:
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: pip install requests websockets pyyaml
      shell: bash

    - name: Run Consensia Review
      env:
        CONSENSIA_API_KEY: ${{ inputs.api-key }}
        GITHUB_TOKEN: ${{ inputs.github-token }}
        TARGET: ${{ inputs.target }}
        MODE: ${{ inputs.mode }}
        ROUNDS: ${{ inputs.rounds }}
        API_URL: ${{ inputs.api-url }}
        FAIL_ON_CRITICAL: ${{ inputs.fail-on-critical }}
        PR_NUMBER: ${{ github.event.issue.number || github.event.pull_request.number || '' }}
        REPO: ${{ github.repository }}
        COMMIT_SHA: ${{ github.sha }}
        GITHUB_BASE_REF: ${{ github.base_ref || '' }}
        GITHUB_EVENT_NAME: ${{ github.event_name }}
        GITHUB_EVENT_PATH: ${{ github.event_path }}
      run: python ${{ github.action_path }}/review.py
      shell: bash